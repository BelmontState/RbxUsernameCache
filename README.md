# RbxUsernameCache

This is a small utility that will cache username lookups to the Roblox API.
Requires .NET 10 and a Redis server running. Also implements an L1 in-memory cache.

Environment variables:
* REDIS_HOST
* REDIS_USERNAME
* REDIS_PASSWORD
* REDIS_DB
